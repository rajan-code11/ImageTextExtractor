import 'package:flutter/material.dart';
import 'batch_crop_page.dart';

class BatchCropWindow extends StatelessWidget {
  const BatchCropWindow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const BatchCropPage();
  }
}